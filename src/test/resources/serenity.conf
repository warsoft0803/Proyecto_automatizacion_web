serenity {

    #Solo toma pantallazos si hay un error
    #take.screenshots = FOR_FAILURES
    #Toma pantallazo por accion realizada es decir cada click
    #take.screenshots = FOR_EACH_ACTION
    #No toma muchas capturar por step
    take.screenshots = AFTER_EACH_STEP
}

headless.mode = false
webdriver {
  driver = chrome
  capabilities {
    browserName = "chrome"
    acceptInsecureCerts = true
    "goog:chromeOptions" {
      args = ["remote-allow-origins=*","test-type", "no-sandbox", "ignore-certificate-errors", "--window-size=1000,800",
        "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", "disable-popup-blocking",
        "disable-dev-shm-usage", "disable-extensions", "disable-web-security", "disable-translate", "disable-logging"]
    }
  }
}

enviroments {

    default{

         webdriver.base.url = "https://www.computrabajo.com/"

      }

    dev {

        # webdriver.base.url = "http://127.0.0.1:5500/dev"
    }

    qa{

    }

}
